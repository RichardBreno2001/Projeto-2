    <section id="encontrar-emprego">
        <div class="container">
            <div class="row">
                <div class="col-12" id="procurar-emprego">
                    <h2>Encontre o emprego que procura</h2>
                    <p>Somos um site com várias vagas direcionadas ao home-office</p>
                    <form action="" autocomplete="off">
                            <div id="btn">
                                <input type="text" id="procurar">
                                <button type="submit" class="btn btn-primary">procurar</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>
    <main>
        <div class="container" id="empregos">
            <div class="row">
                <div class="col-12">
                    <h2>Veja nossas vagas em destaque</h2>
                    <ul>
                        {{#each dados}}
                        <li class=" {{#if dataValues.new_job}}lista-nova{{/if}}">
                            <img src="/1590898.png" alt="imagem">
                            <p>{{dataValues.company}}</p>
                            <h2>{{dataValues.title}}</h2>
                            <p>{{dataValues.salary}}</p>
                            <span class="Nova-vaga">Nova</span>
                            <a href="#" class="btn btn-primary" id="ver">Ver vaga</a>
                        </li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </main>